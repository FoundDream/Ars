interface:
  display_name: "Better Interface"
  short_description: "Build and review complete product interfaces"
  default_prompt: "Use $better-interface to improve or review this interface as one system."
policy:
  allow_implicit_invocation: false
